<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

/**
 * Seeds the `job_batches` table.
 *
 * Populated automatically by Laravel's batched-job feature. No business
 * data to seed.
 */
class JobBatchSeeder extends Seeder
{
    public function run(): void
    {
        // No-op: batched jobs manage this table at runtime.
    }
}
